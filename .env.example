GMAIL_USER=your-gmail@gmail.com
GMAIL_PASS=your-16-char-app-password

# Google Sheets sync — service account key for www-10@wwj-website.iam.gserviceaccount.com
# Download from Google Cloud Console → IAM → Service Accounts → Keys → Add Key → JSON
# Save the downloaded file as .google-credentials.json (it is gitignored)
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=.google-credentials.json
