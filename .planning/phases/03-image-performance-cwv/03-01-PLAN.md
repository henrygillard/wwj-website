---
phase: 03-image-performance-cwv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-images.mjs
  - scripts/convert-images.mjs
  - src/data/photos.js
  - package.json
  - public/photos/barton-springs/hero-1.webp
  - public/photos/barton-springs/hero-2.webp
  - public/photos/barton-springs/DSC06956.webp
  - public/photos/barton-springs/DSC06973.webp
  - public/photos/barton-springs/DSC06978.webp
  - public/photos/barton-springs/DSC06987.webp
  - public/photos/barton-springs/DSC06995.webp
  - public/photos/barton-springs/DSC07023.webp
  - public/photos/barton-springs/DSC07049.webp
  - public/photos/barton-springs/DSC07069.webp
  - public/photos/barton-springs/DSC07123.webp
  - public/photos/barton-springs/DSC07136.webp
  - public/photos/barton-springs/DSC07157.webp
  - public/photos/barton-springs/DSC07164.webp
  - public/photos/barton-springs/DSC07182.webp
  - public/photos/barton-springs/DSC07334.webp
  - public/photos/barton-springs/DSC07401.webp
  - public/photos/barton-springs/DSC07425.webp
  - public/photos/barton-springs/DSC07459.webp
  - public/photos/barton-springs/DSC07479.webp
  - public/photos/radio-east/cover-image.webp
  - public/photos/radio-east/DSC5611.webp
  - public/photos/radio-east/DSC5612.webp
  - public/photos/radio-east/DSC5618.webp
  - public/photos/radio-east/DSC5623.webp
  - public/photos/radio-east/DSC5628.webp
  - public/photos/radio-east/DSC5633.webp
  - public/photos/radio-east/DSC5634.webp
  - public/photos/radio-east/DSC5635.webp
  - public/photos/radio-east/DSC5645.webp
  - public/photos/radio-east/DSC5648.webp
  - public/photos/radio-east/DSC5650.webp
  - public/photos/radio-east/DSC5656.webp
  - public/photos/radio-east/DSC5666.webp
  - public/photos/radio-east/DSC5682.webp
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "All 35 JPEG files in public/photos/ have a corresponding .webp file"
    - "hero-1.webp file size is under 100 KB"
    - "Every other .webp file size is under 120 KB"
    - "src/data/photos.js entries have .webp src paths and width/height fields matching post-resize intrinsic dimensions"
  artifacts:
    - path: "scripts/verify-images.mjs"
      provides: "Automated size + existence check for all 35 WebP files"
      exports: ["exits 0 on pass, exits 1 with failure list on fail"]
    - path: "scripts/convert-images.mjs"
      provides: "Batch JPEG-to-WebP conversion with dimension output"
    - path: "src/data/photos.js"
      provides: "Updated photo entries with .webp paths and width/height fields"
      contains: "\"width\":"
    - path: "public/photos/barton-springs/hero-1.webp"
      provides: "Primary LCP image in WebP format"
  key_links:
    - from: "scripts/convert-images.mjs"
      to: "src/data/photos.js"
      via: "script writes updated photos.js with width/height from sharp output"
      pattern: "writeFileSync.*photos\\.js"
    - from: "src/data/photos.js"
      to: "src/components/Gallery.jsx"
      via: "Gallery imports allPhotos and uses photo.width/photo.height"
      pattern: "photo\\.width"
---

<objective>
Install sharp, write the verify-images.mjs safety scaffold, then write and run convert-images.mjs to convert all 35 JPEG files in public/photos/ to compressed WebP. The conversion script rewrites src/data/photos.js with .webp paths and intrinsic width/height per entry (consumed by Gallery.jsx in Plan 02). After conversion, delete the original .jpg files.

Purpose: PERF-01 (hero under 100 KB) and PERF-02 (all gallery images under 120 KB) require WebP files to exist on disk. photos.js must carry width/height fields before Gallery.jsx can be patched in the next wave.

Output: 35 .webp files in public/photos/, updated src/data/photos.js with dimensions, verify-images.mjs passing cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-image-performance-cwv/03-RESEARCH.md
@.planning/phases/03-image-performance-cwv/03-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From scripts/prerender.mjs (script structure to replicate):
  - JSDoc block comment header
  - bare `node:` import prefix (e.g. `import fs from 'node:fs'`)
  - `const __dirname = path.dirname(fileURLToPath(import.meta.url))`
  - `main().catch((err) => { console.error(err); process.exit(1) })` as final line

From scripts/sync-from-sheet.mjs line 165 (current photos push to replace):
  photoItems.push({ src: row[0], alt: row[1] ?? '', group: row[2] ?? '' })

From scripts/sync-from-sheet.mjs lines 191-196 (photos.js write pattern):
  const photosJs =
    `// Auto-generated — do not edit by hand.\n` +
    `// Run \`npm run sync-content\` to pull updates from Google Sheets.\n\n` +
    `export const allPhotos = ${JSON.stringify(photoItems, null, 2)}\n`
  writeFileSync(resolve(__dirname, '../src/data/photos.js'), photosJs)

From src/data/photos.js (current entry shape — no width/height):
  { "src": "/photos/barton-springs/DSC06956.jpg", "alt": "...", "group": "bartonSprings" }

Target entry shape (after conversion):
  { "src": "/photos/barton-springs/DSC06956.webp", "alt": "...", "group": "bartonSprings", "width": 800, "height": 533 }

Console style (from sync-from-sheet.mjs):
  console.log('✓ src/data/photos.js updated')  // ✓ prefix for success lines
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/verify-images.mjs</name>
  <files>scripts/verify-images.mjs</files>
  <read_first>
    - scripts/prerender.mjs — script structure, JSDoc header, node: imports, __dirname pattern, main().catch() tail
    - .planning/phases/03-image-performance-cwv/03-RESEARCH.md — Wave 0 gap spec, size thresholds (hero-1.webp: 102400 bytes, all others: 122880 bytes), full 35-file inventory
    - .planning/phases/03-image-performance-cwv/03-PATTERNS.md — verify-images.mjs pattern section
  </read_first>
  <action>
    Create scripts/verify-images.mjs as a Node ESM script. Follow the prerender.mjs structure: JSDoc header comment, node: imports, __dirname via fileURLToPath, async main() wrapped in .catch().

    The script checks all 35 WebP files against their size thresholds. Derive the expected file list from the image inventory in RESEARCH.md:
    - barton-springs/hero-1.webp — threshold: 102400 bytes (100 KB)
    - All other 34 files (barton-springs/hero-2.webp, 18 barton-springs DSC*.webp, radio-east/cover-image.webp, 14 radio-east DSC*.webp) — threshold: 122880 bytes (120 KB)

    Logic: for each expected file, check existence with fs.existsSync and size with fs.statSync(path).size. Collect failures into an array. After all checks, if failures.length > 0, print each failure with its actual size and threshold, then process.exit(1). If all pass, print '✓ All 35 WebP images verified — sizes within targets' and process.exit(0).

    Use path.resolve(__dirname, '../public/photos', relativePath) for each file path.

    Do not use watch mode or streaming. Single synchronous pass is correct.
  </action>
  <verify>
    <automated>node /Users/henrygillard/code/wwj-website/scripts/verify-images.mjs 2>&1 | head -5</automated>
  </verify>
  <done>verify-images.mjs exists and runs without a Node syntax error. It will exit 1 (expected — WebP files don't exist yet) but must not throw an uncaught exception. The failure output lists missing files by name.</done>
</task>

<task type="auto">
  <name>Task 2: Install sharp, write and run convert-images.mjs, delete .jpg originals</name>
  <files>scripts/convert-images.mjs, src/data/photos.js, package.json, public/photos/**/*.webp</files>
  <read_first>
    - scripts/prerender.mjs — script structure to replicate (JSDoc, node: imports, __dirname, main().catch())
    - scripts/sync-from-sheet.mjs lines 157-197 — current photos push pattern (line 165) + photos.js write pattern (lines 191-196) to replicate for the updated output
    - src/data/photos.js — current entry shape and full list of 33 photo entries (src, alt, group fields) that must be preserved; array length after conversion must remain 33
    - .planning/phases/03-image-performance-cwv/03-RESEARCH.md — full sharp API patterns (convertToWebP function, resize targets per category, anti-patterns section)
    - .planning/phases/03-image-performance-cwv/03-PATTERNS.md — convert-images.mjs section, photos.js write pattern, console progress style
  </read_first>
  <action>
    Step 1 — Install sharp:
    Run `npm install --save-dev sharp` in the project root. Confirm package.json devDependencies now contains "sharp".

    Step 2 — Write scripts/convert-images.mjs:
    Follow the prerender.mjs script structure. Import sharp from 'sharp'. Import readdir, unlink from 'node:fs/promises'. Import writeFileSync from 'node:fs'. Import path utilities from 'node:path'.

    Define an async convertToWebP(inputPath, maxWidth, quality=75) function that calls:
      sharp(inputPath).resize(maxWidth, null, { fit: 'inside', withoutEnlargement: true }).webp({ quality }).toFile(outputPath)
    Return { outputPath, width: info.width, height: info.height, size: info.size }.

    Resize targets per category (from RESEARCH.md):
    - barton-springs/hero-1.jpg → maxWidth: 1920
    - barton-springs/hero-2.jpg → maxWidth: 1200
    - radio-east/cover-image.jpg → maxWidth: 1200
    - All other gallery images (barton-springs/DSC*.jpg, radio-east/DSC*.jpg) → maxWidth: 800

    The script determines maxWidth by matching the basename against 'hero-1', 'hero-2', and 'cover-image'; all other files use 800.

    Process order: walk public/photos/barton-springs/ then public/photos/radio-east/. For each .jpg file, call convertToWebP and log '✓ converted filename.webp (NNN KB)' per file using the size from info.size.

    After all conversions, rebuild the photos.js content. Read the current photos.js allPhotos array. For each entry, look up the width/height from the conversion results map (keyed by the original .jpg basename). Produce a new array where each entry has:
    - src: original src with .jpg replaced by .webp
    - alt: unchanged
    - group: unchanged
    - width: from conversion result
    - height: from conversion result

    Note: cover-image.jpg is NOT in photos.js (it's CSS-only). The 33 photo entries in photos.js cover only gallery images and hero-1/hero-2; the conversion results map will have their dimensions. Write the updated photos.js using the exact two-line auto-generated header and JSON.stringify(items, null, 2) format from sync-from-sheet.mjs lines 191-196.

    Step 3 — Run the conversion:
    Execute `node scripts/convert-images.mjs` from the project root. Wait for completion. Confirm output shows 35 ✓ lines and the photos.js update line.

    Step 4 — Verify sizes with the scaffold:
    Run `node scripts/verify-images.mjs`. If any file exceeds its threshold, re-run with quality: 65 for the failing file(s) only — but first check: at q75 the barton-springs DSLR shots (4-10 MB, 6000-10000px wide) will compress to well under 120 KB at 800px wide. If verify passes, continue.

    Step 5 — Delete .jpg originals:
    Run `find /Users/henrygillard/code/wwj-website/public/photos -name "*.jpg" -delete` to remove all 35 original JPEG files. Do not delete any .webp files. Do not touch public/logos/*.png — logos are out of scope.

    Step 6 — Confirm:
    Run `find /Users/henrygillard/code/wwj-website/public/photos -name "*.jpg" | wc -l` — must return 0.
    Run `find /Users/henrygillard/code/wwj-website/public/photos -name "*.webp" | wc -l` — must return 35.
    Run `node scripts/verify-images.mjs` — must exit 0.
  </action>
  <verify>
    <automated>node /Users/henrygillard/code/wwj-website/scripts/verify-images.mjs && find /Users/henrygillard/code/wwj-website/public/photos -name "*.jpg" | wc -l</automated>
  </verify>
  <done>verify-images.mjs exits 0. find *.jpg returns 0. find *.webp returns 35. src/data/photos.js entries have .webp src fields and numeric width/height fields. package.json devDependencies includes "sharp".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → script | Input files are developer-controlled JPEG images; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | convert-images.mjs deletes .jpg originals | accept | Originals are tracked in git; git checkout can restore them if needed |
| T-03-02 | Denial of Service | sharp OOM on 10 MB JPEG | accept | sharp streams internally; Node 22 has sufficient heap for 10 MB inputs at 1920px resize |
</threat_model>

<verification>
After both tasks complete:
1. `node scripts/verify-images.mjs` exits 0 — all 35 WebP files exist and are under size targets
2. `find public/photos -name "*.jpg" | wc -l` returns 0 — no JPEG originals remain
3. `find public/photos -name "*.webp" | wc -l` returns 35
4. `grep -c '"width"' src/data/photos.js` returns 33 — every gallery photo entry has a width field
5. `grep -c '"height"' src/data/photos.js` returns 33 — every gallery photo entry has a height field
</verification>

<success_criteria>
- PERF-01: public/photos/barton-springs/hero-1.webp exists and is under 100 KB (102400 bytes)
- PERF-02: All 35 .webp files in public/photos/ exist, each under 120 KB (122880 bytes)
- src/data/photos.js allPhotos array has 33 entries with .webp src paths and numeric width/height fields
- No .jpg files remain in public/photos/
- verify-images.mjs exits 0 cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-performance-cwv/03-01-SUMMARY.md`
</output>
