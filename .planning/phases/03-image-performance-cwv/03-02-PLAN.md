---
phase: 03-image-performance-cwv
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/Gallery.jsx
  - src/components/About.jsx
  - src/components/Nav.jsx
  - src/components/Hero.jsx
  - src/components/VideoSection.jsx
  - src/components/About.module.css
  - scripts/sync-from-sheet.mjs
  - index.html
autonomous: true
requirements:
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "Every <img> in Gallery.jsx has width={photo.width} and height={photo.height} attributes"
    - "Every <img> in About.jsx has explicit numeric width and height attributes"
    - "The <img> in Nav.jsx has width={110} and height={110} attributes"
    - "Hero.jsx and VideoSection.jsx video poster attributes point to hero-1.webp"
    - "About.module.css background-image references cover-image.webp"
    - "index.html contains exactly one <link rel='preload' as='image' fetchpriority='high'> for hero-1.webp"
    - "index.html contains zero references to hero-1.jpg"
    - "sync-from-sheet.mjs normalises .jpg to .webp on photoItems push"
  artifacts:
    - path: "src/components/Gallery.jsx"
      provides: "Gallery img with width/height from data"
      contains: "width={photo.width}"
    - path: "src/components/About.jsx"
      provides: "About stack imgs with .webp paths and explicit dimensions"
      contains: "hero-1.webp"
    - path: "src/components/Nav.jsx"
      provides: "Nav logo img with explicit dimensions"
      contains: "width={110}"
    - path: "index.html"
      provides: "LCP preload hint + updated og/twitter/schema image paths"
      contains: "fetchpriority=\"high\""
    - path: "src/components/About.module.css"
      provides: "CSS background-image updated to .webp"
      contains: "cover-image.webp"
    - path: "scripts/sync-from-sheet.mjs"
      provides: "Idempotent .webp normalisation on future syncs"
      contains: ".replace(/\\.jpg$/i, '.webp')"
  key_links:
    - from: "src/data/photos.js"
      to: "src/components/Gallery.jsx"
      via: "photo.width / photo.height used as img attributes"
      pattern: "photo\\.width"
    - from: "index.html preload href"
      to: "Hero.jsx poster attribute"
      via: "both must be /photos/barton-springs/hero-1.webp exactly"
      pattern: "hero-1\\.webp"
---

<objective>
Patch all source files that reference .jpg image paths or have missing img dimensions. This plan covers: component img attributes (Gallery, About, Nav), video poster paths (Hero, VideoSection), CSS background path (About.module.css), index.html preload hint + three og/twitter/schema path updates, and the sync script .webp normalisation guard.

Purpose: PERF-03 (zero layout shift — all img elements need intrinsic width/height) and PERF-04 (LCP preload hint in index.html). After this plan, every img in the site carries explicit dimensions and every reference to hero-1.jpg is gone.

Output: All 8 files patched. Zero .jpg references in active source code. LCP preload present. CLS-0 achievable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-image-performance-cwv/03-RESEARCH.md
@.planning/phases/03-image-performance-cwv/03-PATTERNS.md
@.planning/phases/03-image-performance-cwv/03-UI-SPEC.md
@.planning/phases/03-image-performance-cwv/03-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From src/components/Gallery.jsx line 36 (current img — no width/height):
  <img src={photo.src} alt={photo.alt} loading="lazy" />

Target Gallery.jsx img (add width/height from data, preserve all other attrs):
  <img src={photo.src} alt={photo.alt} width={photo.width} height={photo.height} loading="lazy" />

From src/components/About.jsx lines 4-5 (current paths — .jpg):
  const hero1 = '/photos/barton-springs/hero-1.jpg'
  const hero2 = '/photos/barton-springs/hero-2.jpg'

From src/components/About.jsx lines 28-39 (current imgs — no width/height):
  <img src={hero1} alt="..." loading="lazy" onClick={() => onOpenPhoto(hero1)} />
  <img src={hero2} alt="..." loading="lazy" onClick={() => onOpenPhoto(hero2)} />

Note: About.jsx img width/height must be taken from the 03-01-SUMMARY.md conversion output.
  hero-1.webp was resized to maxWidth 1920 (portrait: ~1920 wide, exact height from sharp).
  hero-2.webp was resized to maxWidth 1200 (portrait: ~1200 wide, exact height from sharp).
  Use the actual dimensions recorded in the SUMMARY, not estimated values.

From src/components/Nav.jsx line 13 (current logo img — no width/height):
  <img src={logos.whiteTransparent} alt="WWJ" className={styles.logoImg} />

Target Nav.jsx img:
  <img src={logos.whiteTransparent} alt="WWJ" className={styles.logoImg} width={110} height={110} />

From src/components/Hero.jsx line 11 (current poster):
  poster="/photos/barton-springs/hero-1.jpg"

Target Hero.jsx poster:
  poster="/photos/barton-springs/hero-1.webp"

From src/components/VideoSection.jsx line 13 (current poster):
  poster="/photos/barton-springs/hero-1.jpg"

Target VideoSection.jsx poster:
  poster="/photos/barton-springs/hero-1.webp"

From src/components/About.module.css line 5 (current CSS background):
  url('/photos/radio-east/cover-image.jpg') no-repeat center center / cover;

Target About.module.css line 5:
  url('/photos/radio-east/cover-image.webp') no-repeat center center / cover;

From scripts/sync-from-sheet.mjs line 165 (current photos push):
  photoItems.push({ src: row[0], alt: row[1] ?? '', group: row[2] ?? '' })

Target sync-from-sheet.mjs line 165 (normalise extension, add comment):
  photoItems.push({ src: row[0].replace(/\.jpg$/i, '.webp'), alt: row[1] ?? '', group: row[2] ?? '' })

From index.html (current state):
  Line 8:  <link rel="canonical" href="https://wrestlewithjimmy.com/" />
  Line 16: <meta property="og:image" content="https://wrestlewithjimmy.com/photos/barton-springs/hero-1.jpg" />
  Line 22: <meta name="twitter:image" content="https://wrestlewithjimmy.com/photos/barton-springs/hero-1.jpg" />
  Line 34: "image": "https://wrestlewithjimmy.com/photos/barton-springs/hero-1.jpg",

Target index.html additions/changes:
  After line 8 (canonical), insert:
    <link rel="preload" as="image" href="/photos/barton-springs/hero-1.webp" fetchpriority="high" />
  Lines 16, 22, 34: replace hero-1.jpg with hero-1.webp in all three URLs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch Gallery.jsx, About.jsx, Nav.jsx, About.module.css, sync-from-sheet.mjs</name>
  <files>src/components/Gallery.jsx, src/components/About.jsx, src/components/Nav.jsx, src/components/About.module.css, scripts/sync-from-sheet.mjs</files>
  <read_first>
    - src/components/Gallery.jsx — read current file; confirm img element is on line 36 with no width/height
    - src/components/About.jsx — read current file; confirm hero1/hero2 const paths and both img elements
    - src/components/Nav.jsx — read current file; confirm logo img line with no width/height
    - src/components/About.module.css — read current file; confirm cover-image.jpg on line 5
    - scripts/sync-from-sheet.mjs lines 157-170 — confirm current photoItems push on line 165
    - .planning/phases/03-image-performance-cwv/03-01-SUMMARY.md — get actual post-conversion width/height values for hero-1.webp and hero-2.webp from the conversion output (these are the authoritative intrinsic dimensions for About.jsx)
    - .planning/phases/03-image-performance-cwv/03-PATTERNS.md — About.jsx and Gallery.jsx patch sections for exact target state
    - .planning/phases/03-image-performance-cwv/03-UI-SPEC.md — Image Embedding Contract rules 1-7 (especially Rule 3: hero uses video poster, not img; Rule 6: CSS layout unchanged)
  </read_first>
  <action>
    Make five targeted edits:

    1. Gallery.jsx — add width={photo.width} and height={photo.height} to the single img element (line 36). Keep all other attributes (src, alt, loading="lazy") exactly as they are. Width/height are JSX numeric expressions, not string literals.

    2. About.jsx — two changes:
       a. Update const hero1 and const hero2 path strings from .jpg to .webp (lines 4-5).
       b. On both img elements, add width and height numeric attrs. Use the actual intrinsic dimensions from the 03-01-SUMMARY.md sharp conversion output for hero-1.webp (maxWidth 1920, portrait) and hero-2.webp (maxWidth 1200, portrait). Do NOT use estimated values — read the SUMMARY for exact numbers. Keep loading="lazy" and onClick handlers unchanged.

    3. Nav.jsx — add width={110} height={110} to the logo img element (line 13). Preserve className={styles.logoImg} and all other attributes. Do not change the src (logo is PNG, out of scope for WebP conversion).

    4. About.module.css — change cover-image.jpg to cover-image.webp on line 5 (inside the url() background value). Change only the filename extension. All surrounding CSS (linear-gradient, no-repeat, center center / cover) remains identical.

    5. sync-from-sheet.mjs — on line 165, inside the photoItems.push call, change `src: row[0]` to `src: row[0].replace(/\.jpg$/i, '.webp')`. Add an inline comment: `// normalise extension — Phase 3` at the end of that line. Do not change the surrounding guard condition (row[0].startsWith('/') || row[0].startsWith('http')) or the alt/group fields.
  </action>
  <verify>
    <automated>grep -n 'photo\.width' /Users/henrygillard/code/wwj-website/src/components/Gallery.jsx && grep -n 'hero-1\.webp' /Users/henrygillard/code/wwj-website/src/components/About.jsx && grep -n 'width={110}' /Users/henrygillard/code/wwj-website/src/components/Nav.jsx && grep -n 'cover-image\.webp' /Users/henrygillard/code/wwj-website/src/components/About.module.css && grep -n 'replace.*\.jpg' /Users/henrygillard/code/wwj-website/scripts/sync-from-sheet.mjs</automated>
  </verify>
  <done>
    - Gallery.jsx img contains width={photo.width} height={photo.height}
    - About.jsx const hero1 and hero2 use .webp paths; both img elements have numeric width and height attrs
    - Nav.jsx logo img has width={110} height={110}
    - About.module.css uses cover-image.webp in the url() value
    - sync-from-sheet.mjs photoItems push contains .replace(/\.jpg$/i, '.webp')
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch Hero.jsx, VideoSection.jsx, index.html — poster paths and LCP preload</name>
  <files>src/components/Hero.jsx, src/components/VideoSection.jsx, index.html</files>
  <read_first>
    - src/components/Hero.jsx — confirm poster="/photos/barton-springs/hero-1.jpg" on line 11
    - src/components/VideoSection.jsx — confirm poster="/photos/barton-springs/hero-1.jpg" on line 13
    - index.html — read full file; note canonical link (line 8), og:image (line 16), twitter:image (line 22), MusicGroup JSON-LD image field (line 34); confirm the preload link does NOT yet exist
    - .planning/phases/03-image-performance-cwv/03-RESEARCH.md — Pattern 3 (LCP preload hint syntax) and Pattern 4 pitfall notes; Pitfall 4 (preload href must exactly match poster path); Pitfall 6 (og:image and JSON-LD must also be updated)
    - .planning/phases/03-image-performance-cwv/03-PATTERNS.md — index.html patch section (exact target state for all three .jpg refs and the preload insertion point)
  </read_first>
  <action>
    Three targeted edits:

    1. Hero.jsx — change the poster attribute value from "/photos/barton-springs/hero-1.jpg" to "/photos/barton-springs/hero-1.webp". No other changes to this file.

    2. VideoSection.jsx — change the poster attribute value from "/photos/barton-springs/hero-1.jpg" to "/photos/barton-springs/hero-1.webp". No other changes to this file.

    3. index.html — make four changes:
       a. Insert a new line immediately after the canonical link (line 8):
          <link rel="preload" as="image" href="/photos/barton-springs/hero-1.webp" fetchpriority="high" />
          No imagesrcset. No type attribute. The href value is exactly "/photos/barton-springs/hero-1.webp" — matching the poster attributes in Hero.jsx and VideoSection.jsx.
       b. Update og:image content URL: change hero-1.jpg to hero-1.webp in the https://wrestlewithjimmy.com/... URL.
       c. Update twitter:image content URL: same change, hero-1.jpg to hero-1.webp.
       d. Update the MusicGroup JSON-LD "image" field URL: same change, hero-1.jpg to hero-1.webp.

    After editing, verify no hero-1.jpg references remain in index.html:
    Run `grep -c 'hero-1\.jpg' index.html` — must return 0.
    Run `grep -c 'fetchpriority="high"' index.html` — must return 1.
  </action>
  <verify>
    <automated>test $(grep -c 'hero-1\.jpg' /Users/henrygillard/code/wwj-website/index.html) -eq 0 && grep -c 'fetchpriority="high"' /Users/henrygillard/code/wwj-website/index.html && grep -n 'hero-1\.webp' /Users/henrygillard/code/wwj-website/src/components/Hero.jsx && grep -n 'hero-1\.webp' /Users/henrygillard/code/wwj-website/src/components/VideoSection.jsx</automated>
  </verify>
  <done>
    - Hero.jsx poster is "/photos/barton-springs/hero-1.webp"
    - VideoSection.jsx poster is "/photos/barton-springs/hero-1.webp"
    - index.html: grep -c 'hero-1.jpg' returns 0
    - index.html: grep -c 'fetchpriority="high"' returns 1
    - index.html: preload href is /photos/barton-springs/hero-1.webp (no imagesrcset, no type attr)
    - index.html: og:image, twitter:image, and MusicGroup "image" field all reference hero-1.webp
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static HTML → browser | index.html head content is delivered to all clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | og:image / twitter:image in index.html | accept | Hero image is intentionally public; no PII in image metadata |
| T-03-04 | Spoofing | fetchpriority="high" preload hint | accept | Preload hint is a browser hint only; no trust boundary crossed |
</threat_model>

<verification>
After both tasks complete, run the full phase gate:

1. `node scripts/verify-images.mjs` — exits 0 (all 35 WebP files, correct sizes)
2. `grep -c 'hero-1\.jpg' index.html` — returns 0
3. `grep -c 'fetchpriority="high"' index.html` — returns 1
4. `grep -r 'photo\.width' src/components/Gallery.jsx` — returns a match
5. `grep -r '\.jpg' src/components/About.jsx src/components/Hero.jsx src/components/VideoSection.jsx src/components/About.module.css` — returns 0 matches
6. `grep -c 'width={110}' src/components/Nav.jsx` — returns 1
7. `grep -c 'replace.*\.jpg' scripts/sync-from-sheet.mjs` — returns 1
</verification>

<success_criteria>
- PERF-03: Every `<img>` in Gallery.jsx (via photo.width/height), About.jsx (numeric attrs from conversion), and Nav.jsx (110/110) carries explicit width and height attributes
- PERF-04: index.html contains exactly `<link rel="preload" as="image" href="/photos/barton-springs/hero-1.webp" fetchpriority="high" />` immediately after the canonical link
- Zero .jpg references remain in any active source file (components, CSS, index.html)
- sync-from-sheet.mjs normalises future .jpg paths to .webp on photoItems push
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-performance-cwv/03-02-SUMMARY.md`
</output>
