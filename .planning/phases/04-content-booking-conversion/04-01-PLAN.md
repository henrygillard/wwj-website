---
phase: 04-content-booking-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.jsx
autonomous: true
requirements:
  - CONTENT-05
deferred_requirements:
  - CONTENT-01  # deferred per 04-CONTEXT.md <deferred>
  - CONTENT-02  # deferred per 04-CONTEXT.md <deferred>
  - CONTENT-03  # deferred per 04-CONTEXT.md <deferred>
  - CONTENT-04  # deferred per 04-CONTEXT.md <deferred>
  - CONTENT-06  # deferred per 04-CONTEXT.md <deferred>

must_haves:
  truths:
    - "Footer nav contains exactly 6 links in order: About, Gallery, Videos, Shows, Reviews, Book Us"
    - "Videos link points to #video"
    - "Shows link points to #events"
    - "Reviews link points to #testimonials"
    - "Existing links (About #about, Gallery #gallery, Book Us #contact) are unchanged"
  artifacts:
    - path: "src/components/Footer.jsx"
      provides: "Footer nav with all 6 section anchors"
      contains: "#video"
  key_links:
    - from: "Footer nav anchor"
      to: "#video (VideoSection.jsx id)"
      via: "href attribute"
      pattern: 'href="#video"'
    - from: "Footer nav anchor"
      to: "#events (Events.jsx id)"
      via: "href attribute"
      pattern: 'href="#events"'
    - from: "Footer nav anchor"
      to: "#testimonials (Testimonials.jsx id)"
      via: "href attribute"
      pattern: 'href="#testimonials"'
---

<objective>
Add the 3 missing footer nav links (Videos, Shows, Reviews) to Footer.jsx so the footer shows all 6 section anchors as required by CONTENT-05.

Purpose: Event bookers can navigate to any section of the page from the footer, satisfying the footer nav completeness requirement.
Output: Footer.jsx with 6 ordered anchor tags.
</objective>

<execution_context>
@/Users/henrygillard/code/wwj-website/.planning/phases/04-content-booking-conversion/04-CONTEXT.md
</execution_context>

<context>
@/Users/henrygillard/code/wwj-website/.planning/ROADMAP.md
@/Users/henrygillard/code/wwj-website/.planning/REQUIREMENTS.md

<decisions>
D-01: Add the 3 missing nav links directly to Footer.jsx — hardcoded anchors, not sheet-managed.
D-02: Link targets — Videos → #video, Shows → #events, Reviews → #testimonials.
      Final 6-link order: About, Gallery, Videos, Shows, Reviews, Book Us.
</decisions>

<interfaces>
<!-- Current Footer.jsx nav block (read from codebase): -->
<!--
<nav className={styles.links}>
  <a href="#about">About</a>
  <a href="#gallery">Gallery</a>
  <a href="#contact">Book Us</a>
</nav>
-->
<!-- Section IDs confirmed live in codebase:
     #video       → VideoSection.jsx
     #events      → Events.jsx
     #testimonials → Testimonials.jsx
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Videos, Shows, Reviews anchors into Footer nav</name>
  <files>src/components/Footer.jsx</files>
  <read_first>
    - src/components/Footer.jsx — read the full file before editing so the exact current nav block is known
  </read_first>
  <action>
Edit the `<nav className={styles.links}>` block in Footer.jsx to contain all 6 anchors in this exact order (per D-01, D-02):

1. `<a href="#about">About</a>`
2. `<a href="#gallery">Gallery</a>`
3. `<a href="#video">Videos</a>`
4. `<a href="#events">Shows</a>`
5. `<a href="#testimonials">Reviews</a>`
6. `<a href="#contact">Book Us</a>`

Do not add any new className, wrapper element, or style attribute — match the style of the 3 existing anchor elements exactly. Do not touch any other part of the file (logo, tagline, copyright, imports).
  </action>
  <verify>
    <automated>grep -c 'href="#' src/components/Footer.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href="#' src/components/Footer.jsx` outputs `6`
    - `grep 'href="#video"' src/components/Footer.jsx` returns a line containing `>Videos</a>`
    - `grep 'href="#events"' src/components/Footer.jsx` returns a line containing `>Shows</a>`
    - `grep 'href="#testimonials"' src/components/Footer.jsx` returns a line containing `>Reviews</a>`
    - `grep 'href="#about"' src/components/Footer.jsx` still returns the About anchor (unchanged)
    - `grep 'href="#contact"' src/components/Footer.jsx` still returns the Book Us anchor (unchanged)
    - The 6 anchors appear inside `<nav className={styles.links}>` in the order: About, Gallery, Videos, Shows, Reviews, Book Us (verified by reading the file after edit)
    - No new className, wrapper, or style attribute is present on the new anchors
  </acceptance_criteria>
  <done>Footer.jsx nav block contains exactly 6 anchor tags in the specified order, verified by grep count and pattern match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML anchors | No user input; no trust boundary crossed — all hrefs are hardcoded string literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Footer.jsx anchor hrefs | accept | Hardcoded anchor hrefs are static; no user input or dynamic data involved — no tampering surface exists |
</threat_model>

<verification>
After the task completes:

```bash
# Count must be exactly 6
grep -c 'href="#' src/components/Footer.jsx

# Each new anchor must be present
grep 'href="#video"' src/components/Footer.jsx
grep 'href="#events"' src/components/Footer.jsx
grep 'href="#testimonials"' src/components/Footer.jsx
```

Visual check: run `npm run dev`, open the site, scroll to the footer, confirm 6 nav links appear and each scrolls to the correct section.
</verification>

<success_criteria>
- Footer nav shows exactly 6 links: About, Gallery, Videos, Shows, Reviews, Book Us
- Videos → scrolls to #video section
- Shows → scrolls to #events section
- Reviews → scrolls to #testimonials section
- Existing 3 links (About, Gallery, Book Us) are unchanged in text and href
- CONTENT-05 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-booking-conversion/04-01-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
