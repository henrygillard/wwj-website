---
phase: 02-schema-technical-quick-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - public/llms.txt
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04

must_haves:
  truths:
    - "Only the Rock-Box (May 23, 2026) event appears in the JSON-LD ItemList — no past events"
    - "The Rock-Box Offer block contains price and priceCurrency fields with real values"
    - "The canonical tag, og:url meta, and WebSite schema url all end with a trailing slash"
    - "public/llms.txt Upcoming Shows section no longer lists the May 14 Meanwhile Brewing show"
  artifacts:
    - path: "index.html"
      provides: "Corrected JSON-LD ItemList, trailing-slash canonical/og:url/WebSite url"
      contains: "Rock-Box.*position.*1"
    - path: "public/llms.txt"
      provides: "Accurate upcoming show listing"
      excludes: "May 14"
  key_links:
    - from: "index.html"
      to: "Google Rich Results Test"
      via: "JSON-LD script blocks in <head>"
      pattern: "application/ld\\+json"
    - from: "index.html"
      to: "canonical resolution"
      via: "link rel=canonical href"
      pattern: "wrestlewithjimmy\\.com/\""
---

<objective>
Fix all four schema and data-accuracy requirements for Phase 2:
- Remove the past Meanwhile Brewing event from the JSON-LD ItemList (SCHEMA-01)
- Add price and priceCurrency to the Rock-Box Offer block (SCHEMA-02)
- Add trailing slash to canonical, og:url, and WebSite schema url (SCHEMA-03)
- Remove the May 14 show from llms.txt Upcoming Shows (SCHEMA-04)

Purpose: Structured data errors suppress rich results in Google Search. Stale show data misleads
LLM crawlers and users. All four fixes are surgical text edits to two files — no new
dependencies, no build changes required.

Output: index.html with corrected JSON-LD (single Rock-Box event at position 1, full Offer
block, trailing-slash URLs); public/llms.txt with accurate Upcoming Shows section.
</objective>

<execution_context>
@/Users/henrygillard/code/wwj-website/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/henrygillard/code/wwj-website/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/henrygillard/code/wwj-website/.planning/PROJECT.md
@/Users/henrygillard/code/wwj-website/.planning/ROADMAP.md
@/Users/henrygillard/code/wwj-website/.planning/STATE.md

<interfaces>
<!-- Key facts extracted from source files — no codebase exploration needed -->

From index.html (current state):
- Three <script type="application/ld+json"> blocks in <head>: WebSite, MusicGroup, ItemList
- ItemList currently has TWO events: Meanwhile Brewing (position 1) and Rock-Box (position 2)
- Rock-Box Offer block currently has ONLY: url + availability (missing price + priceCurrency)
- canonical href: "https://wrestlewithjimmy.com" (no trailing slash)
- og:url content: "https://wrestlewithjimmy.com" (no trailing slash)
- WebSite schema "url": "https://wrestlewithjimmy.com" (no trailing slash)
- sitemap.xml already has trailing slash — DO NOT TOUCH sitemap.xml

From public/llms.txt (current state):
- Upcoming Shows section lists two entries:
  - "May 14, 2026 — Meanwhile Brewing Company, Austin TX (free, ticketed via Prekindle)"  ← DELETE THIS LINE
  - "May 23, 2026 — The Rock-Box, San Antonio TX (ticketed via Stubwire)"  ← KEEP

From src/data/events.js:
- Meanwhile Brewing has upcoming: false → confirms it is a past event
- Rock-Box has no price field → price must be fetched from Stubwire URL before writing

Stubwire ticket URL: https://www.stubwire.com/e/37806/anthem182/therockbox/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Look up Rock-Box ticket price, then rewrite JSON-LD ItemList in index.html (SCHEMA-01, SCHEMA-02)</name>
  <files>index.html</files>

  <read_first>
    /Users/henrygillard/code/wwj-website/index.html
    — Read the full file to see the current ItemList JSON-LD block before touching anything.
    Then fetch https://www.stubwire.com/e/37806/anthem182/therockbox/ to obtain the ticket price.
  </read_first>

  <action>
    Step 1 — Price lookup: Use WebFetch or curl to retrieve https://www.stubwire.com/e/37806/anthem182/therockbox/.
    Find the ticket price displayed on the page (look for dollar amounts near "Buy Tickets" or ticket tiers).
    Record the numeric value (e.g., "10" for $10, "0" if free). Use "USD" for priceCurrency.

    Step 2 — Rewrite the ItemList block in index.html:
    Locate the <script type="application/ld+json"> block that contains "@type": "ItemList".
    Replace the entire itemListElement array with a single entry for Rock-Box only:
    - "@type": "Event"
    - "position": 1  (renumbered from 2 — Meanwhile Brewing entry is deleted entirely)
    - "name": "Wrestle With Jimmy at The Rock-Box"
    - "startDate": "2026-05-23"
    - "eventStatus": "https://schema.org/EventScheduled"
    - "eventAttendanceMode": "https://schema.org/OfflineEventAttendanceMode"
    - location: { "@type": "Place", "name": "The Rock-Box", address: { "@type": "PostalAddress", "addressLocality": "San Antonio", "addressRegion": "TX", "addressCountry": "US" } }
    - performer: { "@id": "https://wrestlewithjimmy.com/#musicgroup" }
    - organizer: { "@id": "https://wrestlewithjimmy.com/#musicgroup" }
    - offers: { "@type": "Offer", "url": "https://www.stubwire.com/e/37806/anthem182/therockbox/", "price": "<VALUE FROM STEP 1>", "priceCurrency": "USD", "availability": "https://schema.org/InStock" }
    - "url": "https://www.stubwire.com/e/37806/anthem182/therockbox/"

    The Meanwhile Brewing entry (position 1, "name": "Wrestle With Jimmy at Meanwhile Brewing Company",
    "startDate": "2026-05-14") must be completely removed — no trace in the JSON-LD output.

    Do NOT modify the WebSite or MusicGroup JSON-LD blocks in this task (those are handled in Task 2).
    Do NOT modify prerender.mjs — schema lives in index.html directly.
  </action>

  <verify>
    <automated>
      npm run build --prefix /Users/henrygillard/code/wwj-website 2>&1 | tail -5 &amp;&amp; grep "Meanwhile Brewing" /Users/henrygillard/code/wwj-website/dist/index.html | wc -l | xargs -I{} sh -c 'test {} -eq 0 &amp;&amp; echo "PASS: Meanwhile Brewing absent" || echo "FAIL: Meanwhile Brewing still present"'
    </automated>
    <automated>
      grep -c '"position": 1' /Users/henrygillard/code/wwj-website/index.html | xargs -I{} sh -c 'test {} -ge 1 &amp;&amp; echo "PASS: position 1 present" || echo "FAIL: no position 1"'
    </automated>
    <automated>
      grep -o '"price": "[^"]*"' /Users/henrygillard/code/wwj-website/index.html | head -1
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "Meanwhile Brewing" /Users/henrygillard/code/wwj-website/index.html returns 0 matches (string completely absent from the file)
    - The ItemList block contains exactly one Event entry with "position": 1 and "name": "Wrestle With Jimmy at The Rock-Box"
    - The Rock-Box offers object contains "price": "&lt;non-empty value&gt;" and "priceCurrency": "USD" — both fields present as quoted strings in the JSON
    - "startDate": "2026-05-14" does NOT appear anywhere in index.html
    - npm run build exits 0 (no syntax errors)
  </acceptance_criteria>

  <done>
    Meanwhile Brewing entry fully removed from ItemList. Rock-Box is the only event at position 1.
    Rock-Box Offer block has price (from Stubwire lookup) and priceCurrency: "USD". Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add trailing slash to canonical, og:url, and WebSite schema url in index.html (SCHEMA-03); remove past show from llms.txt (SCHEMA-04)</name>
  <files>index.html, public/llms.txt</files>

  <read_first>
    /Users/henrygillard/code/wwj-website/index.html
    — Verify current state of canonical, og:url, and WebSite schema url after Task 1 edits.
    /Users/henrygillard/code/wwj-website/public/llms.txt
    — Read the full file to confirm the "May 14" line location and surrounding content.
  </read_first>

  <action>
    Part A — index.html trailing-slash fixes (three edits):

    1. Canonical tag: Change
         href="https://wrestlewithjimmy.com"
       to
         href="https://wrestlewithjimmy.com/"
       (the link element with rel="canonical" in &lt;head&gt;)

    2. og:url meta: Change
         content="https://wrestlewithjimmy.com"
       to
         content="https://wrestlewithjimmy.com/"
       (the meta element with property="og:url" in &lt;head&gt;)

    3. WebSite schema url field: In the JSON-LD block with "@type": "WebSite", change
         "url": "https://wrestlewithjimmy.com"
       to
         "url": "https://wrestlewithjimmy.com/"

    Scope limit: Do NOT change the MusicGroup "@type" block's "url" field — SCHEMA-03 explicitly
    lists only canonical, og:url, WebSite schema url, and sitemap loc. MusicGroup url is out of scope
    per the requirement text. Also do NOT touch sitemap.xml (already correct).

    Part B — public/llms.txt cleanup:

    Locate the "## Upcoming Shows" section. Delete the entire line:
      "May 14, 2026 — Meanwhile Brewing Company, Austin TX (free, ticketed via Prekindle)"
    The "May 23, 2026 — The Rock-Box, San Antonio TX (ticketed via Stubwire)" line must remain unchanged.
    Do not alter any other section or line in llms.txt.
  </action>

  <verify>
    <automated>
      grep -c 'wrestlewithjimmy\.com/"' /Users/henrygillard/code/wwj-website/index.html
    </automated>
    <automated>
      grep "May 14" /Users/henrygillard/code/wwj-website/public/llms.txt | wc -l | xargs -I{} sh -c 'test {} -eq 0 &amp;&amp; echo "PASS: May 14 absent from llms.txt" || echo "FAIL: May 14 still present"'
    </automated>
    <automated>
      grep "May 23" /Users/henrygillard/code/wwj-website/public/llms.txt | wc -l | xargs -I{} sh -c 'test {} -ge 1 &amp;&amp; echo "PASS: Rock-Box entry present" || echo "FAIL: Rock-Box entry missing"'
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c 'wrestlewithjimmy.com/"' /Users/henrygillard/code/wwj-website/index.html returns 3 (canonical href, og:url content, WebSite "url" — all three end with trailing slash)
    - grep "May 14" /Users/henrygillard/code/wwj-website/public/llms.txt returns no output (zero matches)
    - grep "May 23" /Users/henrygillard/code/wwj-website/public/llms.txt returns the Rock-Box line (still present)
    - grep "Meanwhile Brewing" /Users/henrygillard/code/wwj-website/public/llms.txt returns no output (zero matches)
    - The MusicGroup "url" field in index.html is NOT modified (still "https://wrestlewithjimmy.com" without slash, per SCHEMA-03 scope)
    - npm run build exits 0
  </acceptance_criteria>

  <done>
    Three trailing-slash edits applied in index.html. May 14 show line deleted from llms.txt.
    May 23 Rock-Box line intact. Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL fetch | Stubwire ticket page is a third-party URL; executor fetches it read-only to obtain price — no user data transmitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | index.html JSON-LD | accept | Static file served by Express; no runtime generation in Phase 2; schema is editor-controlled content |
| T-02-02 | Information Disclosure | public/llms.txt | accept | File is intentionally public (served for LLM crawlers); no PII or secrets in scope |
</threat_model>

<verification>
After both tasks complete, run the full suite of smoke checks:

```bash
# Build must succeed
npm run build --prefix /Users/henrygillard/code/wwj-website

# SCHEMA-01: Meanwhile Brewing absent from built HTML
grep "Meanwhile Brewing" /Users/henrygillard/code/wwj-website/dist/index.html && echo "FAIL" || echo "PASS: SCHEMA-01"

# SCHEMA-02: price field present near Rock-Box
node -e "const h=require('fs').readFileSync('/Users/henrygillard/code/wwj-website/dist/index.html','utf8'); const m=h.match(/Rock-Box[\s\S]{0,600}\"price\"/); console.log(m?'PASS: SCHEMA-02':'FAIL: SCHEMA-02 missing price')"

# SCHEMA-03: three trailing-slash URLs in source index.html
grep -c 'wrestlewithjimmy\.com/"' /Users/henrygillard/code/wwj-website/index.html

# SCHEMA-04: May 14 absent from llms.txt
grep "May 14" /Users/henrygillard/code/wwj-website/public/llms.txt && echo "FAIL" || echo "PASS: SCHEMA-04"
```

All four checks must pass before creating the SUMMARY.
</verification>

<success_criteria>
1. Google Rich Results Test shows only the Rock-Box event (May 23) — no past events in EventScheduled schema
2. Rock-Box Offer block in rendered JSON-LD includes "price" (numeric string, non-empty) and "priceCurrency": "USD"
3. grep -c 'wrestlewithjimmy.com/"' index.html returns 3 — canonical, og:url, and WebSite url all have trailing slash
4. public/llms.txt Upcoming Shows section contains only the May 23 Rock-Box line; May 14 Meanwhile Brewing line is gone
5. npm run build exits 0
</success_criteria>

<output>
After completion, create `/Users/henrygillard/code/wwj-website/.planning/phases/02-schema-technical-quick-fixes/02-01-SUMMARY.md`
</output>
